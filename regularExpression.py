import re


# s = 'GeeksforGeeks: A computer science portal for geeks'

# re.findall() finds and returns all matching occurrences in a list

# match = re.search(r'portal', s)

# match = re.findall(r'portal', s)

# match = re.split(r'portal', s)

# match = re.sub(r'portal', 'replace', s)

# match = re.subn('portal', 'new word', s)

# match = re.escape(s)

# match = re.search('portal', s)




